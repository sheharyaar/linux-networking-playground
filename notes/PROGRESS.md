# PROGRESS — packet-path dossier (chapters 8-16 handover)

Format: one line per completed unit. Appended = in packet-path-dossier.html and audited clean.

- 2026-09-18 ch7 fix: reworded the BANNED "unlock" hit to "when it releases the lock" in build/ch07.html and the dossier. Audit BANNED now empty. Confirmed the 12 em dashes are all figcaption "Diagram N.M —" conventions (count tracks the diagram count exactly), which the style spec allows.
- 2026-09-18 bench: pwru 1.0.12 in kind-worker has NO --filter-dst-ip/--filter-dst-port; filtering is by pcap expression as a positional arg. Recorded in notes/traces.md. The /tmp GRO transcripts were lost at reboot but the two veth door sequences were already written into notes/traces.md.
- 2026-09-18 ch8 traced: (a) lo path in the node root netns against the already-running cilium health listener on 172.18.0.4:4240, 1631 lines, one skb address, four nf_hook_slow, NO input route lookup; (b) mechanism for that verified in source (skb_dst_force loopback.c:85 + RTCF_LOCAL route.c:1693 + skb_valid_dst guard ip_input.c:406); (c) tap-on-lo question settled by measurement with a throwaway AF_PACKET socket: every packet seen TWICE, PACKET_OUTGOING then PACKET_HOST, 24 frames for 12 packets; (d) same-pod demo with a throwaway ns pp-demo / pod pp-twins, 390 events, all in the pod netns on lo, zero hits for veth_xmit / cil_ / nf_hook_slow / lxc. Pod and namespace deleted, temp files removed.
- 2026-09-18 ch8 APPENDED. 2532 words, 1 diagram (8.1), 3 questions with answers, 4 refs. Audit: 0 early uses of 149 terms, no nesting errors, BANNED empty. Terms remapped: RTCF_LOCAL and rt_dst_alloc belong to ch4 (defined there); unix_stream_sendmsg, unix_dgram_sendmsg and sock_alloc_send_pskb belong to ch0 (named and defined in a ch0 check answer).
- PENDING: ch9-ch16, glossary, references, reading times, hero totals, lab scripts 11/12/13/14/15 + README, repo README, memory status line.
- 2026-09-18 ch9 traced: rebuilt pp-a/pp-b veth lab inside kind-worker via docker exec. Captured all three states with real skb addresses: default backlog door; GRO on receiver only (still backlog, proving the eligibility rule reads the SENDER's TSO); GRO + sender tso/gso off (veth_xdp_rcv_skb, GRO chain, no backlog). Confirmed one skb address across the netns boundary and that ip_route_input_slow DOES run here (contrast with ch8 lo). Verified skb_scrub_packet's exact clear list at skbuff.c:6278-6299. CORRECTION to the brief: tc_index is NOT cleared by skb_scrub_packet in v7.2, and skb->dev is swapped by eth_type_trans, not by the scrub. Topology and features restored, everything deleted.
- 2026-09-18 ch9 APPENDED. 2343 words, 3 diagrams (9.1 doors, 9.2 namespaces, 9.3 the copy), 3 questions, 4 refs. Audit: 0 early uses of 162 terms, BANNED empty, em dashes 16 = figure count 16 (all figcaptions). Terms remapped: veth_xmit and __dev_forward_skb first named in ch0, veth_xdp_rcv_skb in ch2. Verified every such term still appears in exactly one .words dl, so the merged glossary stays unique.
- 2026-09-18 ch10 traced: built pp-l/pp-br0/pp-r bridge lab in kind-worker. One skb address across three namespaces, device column flipping at br_forward_finish, and ZERO nf_hook_slow in the whole capture. Learned fdb captured. br_handle_frame itself is unprobeable by pwru (double-pointer signature); the first bridge function visible is br_handle_frame_finish. JUDGMENT CALL: did not modprobe br_netfilter, because br_nf_ops registration is global and bridge-nf-call-iptables defaults to 1, which would have changed packet handling for br-kind-cilium carrying the live cluster. That half is labelled expected and derived from verified source. Lab torn down.
- 2026-09-18 ch10 APPENDED. 2497 words, 2 diagrams, 3 questions, 4 refs. Audit clean: 0 early uses of 183 terms. br_handle_frame and br_netfilter remapped to ch3 (first named there). Added notes/check_dl.py: 114 word-list terms so far, 0 duplicated across chapters.
- 2026-09-18 ch11 traced: built the bond (pp-bond0 active-backup over pp-s1/pp-s2, peers bridged in pp-peer) and the VRF (pp-vrf table 10 with pp-ve) in kind-worker. Bond: captured TWO __dev_queue_xmit passes for one skb and the receive-side device swap (RX_HANDLER_ANOTHER) between __netif_receive_skb_one_core and ip_rcv. VRF: captured PRE_ROUTING running TWICE on ingress (pp-ve then pp-vrf) and FOUR nf_hook_slow calls on egress, and matched all four to vrf_ip_out_direct:911/913 and vrf_output_direct:884. Confirmed bond_handle_frame and br_handle_frame are unprobeable by pwru (double-pointer signature). Everything torn down, table 10 flushed.
- 2026-09-18 FIXED a real error in ch4: it cited l3mdev_ip_rcv at ip_input.c line 485; the call is at line 486. Corrected in build/ch04.html and the dossier.
- 2026-09-18 ch11 APPENDED. 2275 words, 2 diagrams, 3 questions, 4 refs. Audit clean, 0 early uses of 204 terms, 126 word-list terms with 0 duplicates. lab/11-bond-vrf.sh written (syntax-checked).
- 2026-09-18 ch12 traced: macvlan pp-mv0 on pp-lo0 and ipvlan pp-iv0 on pp-lo2 in kind-worker. Measured the MAC and promiscuity difference directly (macvlan: own MAC, lower promiscuity 1; ipvlan: same MAC, lower promiscuity 0). Discovered and verified the one-rx_handler-per-device rule (EBUSY at net/core/dev.c:5913) by trying to stack both on one lower. The two receive traces differ by exactly ONE line: macvlan_forward_source vs ipvlan_get_L3_hdr. Verified the l3 outbound path (parent namespace) and the l3s LOCAL_IN hook in source. Torn down.
- 2026-09-18 ch12 APPENDED. 2320 words, 1 diagram, 3 questions, 3 refs. Verified 3 new URLs with WebFetch and added them to notes/refs-verified.md (ip-link(8), ipvlan.html, Docker macvlan driver). lab/12-macvlan-ipvlan.sh written.
- 2026-09-18 ch13: NOT traced, labelled expected throughout, as planned. CORRECTION to the brief: the ip_vs module DOES load on this kernel (modprobe ip_vs succeeded in kind-worker). It was loaded once to check, found inert with no virtual services, and rmmod'd again; nf_conntrack refcount returned from 8 to 7, so the bench is back to its prior state. It still could not be exercised because ipvsadm is absent on both host and node and installing it is out of scope. All ch13 anchors re-verified against v7.2, using the CURRENT names: ip_vs_ops4[]/ip_vs_ops6[] (not ip_vs_ops), ip_vs_in_hook:2097 and ip_vs_out_hook:1510 (not ip_vs_in/ip_vs_reply4, which no longer exist).
- 2026-09-18 ch13 APPENDED. 2336 words, 1 diagram, 3 questions, 4 refs. Fixed a banned metaphor ("bites") in a ref annotation and narrowed the TERMS key "director" to "the director" because it substring-matched "directory" in ch10. Audit clean: 0 early uses of 237 terms, 147 word-list terms, 0 duplicates. lab/13-ipvs.sh written.
- 2026-09-18 ch14/ch15 traced on the live cluster (read-only). Created throwaway ns pp-demo with pp-cli/pp-srv on kind-worker and pp-cli-far on kind-worker2 (no VPC labels, so no CiliumNetworkPolicy selects them). CORRECTIONS to the brief: node IPs changed (worker2 = 172.18.0.2, control-plane = 172.18.0.3); acme/far-client is DENIED to acme/server by the fork's security-group policy (captured as SKB_DROP_REASON_TC_INGRESS, used as a bonus trace in ch14); and world traffic does NOT pass to stack on this cluster because BPF host routing takes fib_redirect_v4 (bpf_lxc.c:1547/1550) instead of pass_to_stack (:1565) - measured 0 nf_hook_slow inside the node. Cross-node: one skb address across two nodes end to end, and 116 of 116 node-netns hook calls carried the OUTER geneve tuple, 0 the inner. Verified skb_do_redirect BPF_F_PEER branch at filter.c:2531-2542 and the TC_ACT_REDIRECT/-EAGAIN/another_round chain at dev.c:4493-4502 and 6069.
- 2026-09-18 ch14 APPENDED (2379 words, 2 diagrams) and ch15 APPENDED (2594 words, 2 diagrams), both 3 questions and 4 refs. Verified 4 Cilium doc URLs with WebFetch (note: lifeofapacket lives under network/ebpf/, not network/concepts/, which 404s). Audit clean: 0 early uses of 275 terms, 169 word-list terms, 0 duplicates. lab/14-cilium.sh, lab/15-kind-vxlan.yaml, lab/15-kind-native.yaml written; the two extra clusters were deliberately NOT created.
- 2026-09-18 ch16 APPENDED (1624 words, 1 diagram, 2 questions, 5 refs). Contains the master table of all 12 scenarios, each row linking to its chapter; the flowtable/tcx/netkit/BIG TCP section; and an explicit list of what was NOT established.
- 2026-09-18 CLOSING TASKS done. build/glossary.html generated by notes/build_glossary.py (173 terms, grouped by chapter, each heading linking back, 0 duplicates per notes/check_dl.py). build/references.html generated by notes/build_references.py (68 unique URLs, grouped into all 6 layers, each carrying the chapters that cite it; generated FROM the chapters so no unverified URL can appear). Both appended; the <!--APPEND--> marker is gone. notes/patch_times.py replaced all 34 RTn placeholders with measured minutes and the hero totals with ~40,873 words / ~206 minutes / 27 diagrams; removed a duplicate back-matter TOC group that the skeleton already had and relabelled the TOC times as "N min".
- 2026-09-18 FINAL AUDIT CLEAN: 27 figures = 27 mermaid = 27 captions, 17 of each block, 0 leftover markers, 50 questions and 50 answers with none thin, reveal-all and beforeprint present, all 27 mermaid headers valid with no entity or paren warnings, no HTML nesting errors, 0 early uses of 282 terms, BANNED and METAPHOR both empty, 27 em dashes = exactly the 27 figcaptions. 40,873 chapter words.
- 2026-09-18 lab/README.md (prereq table with packages, host/node/cluster column per lab, kind copy commands, pwru traps) and the repo README.md written. Labs synced into kind-worker:/root/lab. Memory status line appended.
- 2026-09-18 BENCH LEFT AS FOUND: 0 pp- links, 0 pp- netns, table 10 empty, ip_vs unloaded, pp-demo namespace deleted, all 32 cluster pods Running, no change to the kind cluster or Cilium config.
