# Cilium datapath anchors — fork ~/workspace/neverinstall/cilium v1.19.6-vpc.18 (upstream-equivalent names noted). Live cluster: image localhost:5000/cilium/cilium-dev:local.

Entry programs use `__section_entry` (bpf/include/bpf/section.h:21 → section "tc/entry"). Tail calls via __declare_tail(CILIUM_CALL_*).

## bpf_lxc.c (pod egress)
cil_from_container :2211 (attached lxc ingress = packets leaving pod; bpf_clear_meta; tail_call_internal CILIUM_CALL_IPV4_FROM_LXC) → tail_handle_ipv4 :2156 → __tail_handle_ipv4 :2070 → __per_packet_lb_svc_xlate_4 :167 (lb4_lookup_service :202) or tail_ipv4_ct_egress :2066 → tail_handle_ipv4_cont :2050 → handle_ipv4_from_lxc :1591 (ipcache_lookup4 :1715 fills dst_sec_identity; WORLD_IPV4_ID if miss :1722; policy_can_egress4 :1899) → ipv4_forward_to_destination :1187: local endpoint branch 1346-1424 (lookup endpoint → ipv4_local_delivery :1398-1424); TUNNEL_MODE branch 1460-1545 (encap_and_redirect_lxc :1532 when info->flag_has_tunnel_ep); ENABLE_HOST_ROUTING native branch 1547-1562 (fib_redirect_v4 :1550); pass_to_stack: 1565-1583 → CTX_ACT_OK.
cil_to_container :3079 (lxc egress, only when endpoint routes on) → CILIUM_CALL_IPV4_CT_INGRESS → tail_ipv4_policy :2797 (ipv4_policy :2595, policy_can_ingress4 :2707; redirect_ep :2862 when do_redirect).

## bpf/lib/local_delivery.h
tail_call_policy :43 (tail call cilium_call_policy[ep->lxc_id]); should_fast_redirect :59 (ENABLE_HOST_ROUTING && !from_host && ingress_ifindex>0, :76-78); redirect_ep :82-96 (ctx_redirect if !fast; else ctx_change_type(PACKET_HOST) if from_tunnel; ctx_redirect_peer); local_delivery :133-195 (USE_BPF_PROG_FOR_INGRESS_POLICY unset → tail_call_policy :194); ipv4_local_delivery :229-247 (ipv4_l3 rewrites MACs + dec TTL; bpf/lib/l3.h:37-54).
ctx_redirect → redirect(); ctx_redirect_peer → redirect_peer() — bpf/include/bpf/ctx/skb.h:88-98.
fib_redirect_v4 bpf/lib/fib.h:263 → fib_do_redirect :86 → redirect_neigh :115/:121; ipv4_l3 at fib.h:283.

## encap — bpf/lib/encap.h, overloadable_skb.h
encap_and_redirect_lxc :381-388 → encap_and_redirect_with_nodeid :312 → __encap_and_redirect_with_nodeid :254-279 (__encap_with_nodeid4 :165-209 sets *ifindex = ENCAP_IFINDEX :184; ctx_redirect(ctx, ifindex, 0) :278). ctx_set_encap_info4 bpf/lib/overloadable_skb.h:189-222: key.tunnel_id = get_tunnel_id(seclabel); key.remote_ipv4; ctx_set_tunnel_key (= skb_set_tunnel_key, skb.h:48) with BPF_F_ZERO_CSUM_TX; ctx_set_tunnel_opt for Geneve TLV. Identity rides in the VNI for both VXLAN and Geneve; Geneve additionally carries a TLV option (upstream: DSR; fork: VPC). Ports: VXLAN 8472, Geneve 6081 (pkg/defaults/defaults.go:475/477). TUNNEL_PROTOCOL defines bpf/node_config.h:27-31; emitted by pkg/datapath/tunnel/tunnel.go:204-206.

## bpf_overlay.c
cil_from_overlay :650-654 (cilium_geneve ingress; get_tunnel_key :718 → src identity = get_id_from_tunnel_id :723; store CB_SRC_LABEL) → tail_handle_ipv4 :570 → handle_ipv4 :287 → lookup_ip4_endpoint → ipv4_local_delivery(from_tunnel=true) :559 or ipv4_host_delivery :198.
cil_to_overlay :781-784 (egress cilium_geneve; handle_nat_fwd for NodePort SNAT unless snat_done :825-841).

## bpf_host.c
cil_from_netdev :1290-1298 (eth0 ingress; attached when host fw / NodePort / L2 announce) → do_netdev :1127 → handle_ipv4 :599 (nodeport_lb4 :630) → handle_ipv4_cont :684 → local ep → ipv4_local_delivery :889; ipcache :929; encap from host :951. NO TUNNEL_PORT handling in bpf_host: inbound Geneve UDP passes to the stack → geneve socket → cilium_geneve → cil_from_overlay. Raw rule: CILIUM_PRE_raw -p udp --dport 6081 -j CT --notrack.
cil_from_host :1404 (cilium_host egress). cil_to_netdev :1501-1506 (eth0 egress; handle_nat_fwd :1707 → bpf/lib/nodeport_egress.h snat_v4_needs_masquerade :382 / snat_v4_nat :407; gate ENABLE_MASQUERADE_IPV4 :358). cil_to_host :1849 (cilium_host and cilium_net ingress).

## maps
cilium_lxc bpf/lib/eps.h:61 (lookup_ip4_endpoint :92); cilium_ipcache_v2 LPM :183 (ipcache_lookup4 :217; lookup_ip4_remote_endpoint macro :250); cilium_policy_v2 bpf/lib/policy.h:165 (__policy_can_access :216; policy_can_ingress4 :420; policy_can_egress4 :478); cilium_ct4_global bpf/lib/conntrack_map.h:111 (ct_lookup4 conntrack.h:1058; ct_create4 :1170); snat_v4_nat bpf/lib/nat.h:970; cilium_lb4_services_v2 bpf/lib/lb.h:279 (lb4_lookup_service :1730).

## attachment (pkg/datapath/loader)
cil_from_container → lxcXXXX tc ingress (endpoint.go:219); cil_to_container → lxc egress only if RequireEgressProg (endpoint.go:224-231); cil_from_netdev → eth0 ingress (host.go:249); cil_to_netdev → eth0 egress (host.go:255); cil_from_overlay → cilium_geneve ingress (overlay.go:69); cil_to_overlay → egress (overlay.go:73); cil_to_host → cilium_host ingress (host.go:105) and cilium_net ingress (host.go:165); cil_from_host → cilium_host egress (host.go:110). All via attachSKBProgram(..., option.Config.EnableTCX). enable-tcx option pkg/option/config.go:346. cilium_host/cilium_net veth pair: setupBaseDevice pkg/datapath/loader/netlink.go:118-124; cilium_host carries router IP (:153); ARP off (:136-142).
Options: enable-host-legacy-routing (config.go:213; default false, defaults.go:210; BPFHostRoutingEnabled = !EnableHostLegacyRouting config.go:2111 → ENABLE_HOST_ROUTING). routing-mode native|tunnel (config.go:428, :1011/:1014; validated :2190). tunnel-protocol vxlan|geneve (tunnel.go:280; default vxlan defaults.go:444). ENABLE_ROUTING per endpoint (config.go:877). USE_BPF_PROG_FOR_INGRESS_POLICY when EnableEndpointRoutes or RequireEgressProg (config.go:850/873).

## live cluster (2026-09-17)
RoutingMode tunnel, geneve, TUNNEL_PORT 6081, ENCAP_IFINDEX 5 = cilium_geneve (ip -d: "geneve external ... dstport 6081"); DatapathMode veth; Devices [eth0]; EnableTCX true (Attach Mode: TCX); EnableHostLegacyRouting false (Host: BPF); BPF masquerade on eth0; EnableEndpointRoutes false → no cil_to_container attached, no per-pod routes; KPR true; L7 proxy enabled but unused; XDP off. bpftool net: eth0 tcx ingress cil_from_netdev / egress cil_to_netdev; cilium_net ingress cil_to_host; cilium_host ingress cil_to_host / egress cil_from_host; cilium_geneve ingress cil_from_overlay / egress cil_to_overlay; lxc* ingress cil_from_container. Routes: remote pod CIDRs via 10.128.0.75 dev cilium_host mtu 1442 (=1500-58); tunnel entered by BPF redirect, not by route. iptables present (kube-proxy pods still run) but Cilium's socket LB/BPF LB act first; raw NOTRACK udp 6081. Node has no bpftool/tcpdump/ping; agent image has bpftool. Pods on kind-worker: acme/far-client 10.64.1.13, acme/server 10.64.1.10, globex/server 10.64.1.10 (fork VPC overlap), acme/locked 10.64.1.12; worker2: acme/client 10.64.1.11, default/masq-client 10.128.0.51; default/masq-witness hostNetwork 172.18.0.4. Nodes: cp 172.18.0.2, worker2 172.18.0.3, worker 172.18.0.4.
